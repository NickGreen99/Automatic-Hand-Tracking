# Core dependencies
numpy>=1.22
scipy>=1.7
matplotlib>=3.10
mediapipe==0.10.20
opencv-contrib-python==4.11.0.86

# PyTorch for CUDA-enabled systems
torch==2.5.1+cu118; platform_system != "Darwin"
torchvision==0.20.1+cu118; platform_system != "Darwin"
torchaudio==2.5.1+cu118; platform_system != "Darwin"

# PyTorch for macOS (CPU-only)
torch==2.5.1; platform_system == "Darwin"
torchvision==0.20.1; platform_system == "Darwin"
torchaudio==2.5.1; platform_system == "Darwin"

# Platform-specific dependencies
pywin32==308; platform_system == "Windows"
sounddevice==0.5.1; platform_system != "Windows"

# Git-based dependencies
SAM-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4#
numpy>=1.22
scipy>=1.7
matplotlib>=3.10
mediapipe==0.10.20
opencv-contrib-python==4.11.0.86

# PyTorch for CUDA-enabled systems
torch==2.5.1+cu118; platform_system != "Darwin"
torchvision==0.20.1+cu118; platform_system != "Darwin"
torchaudio==2.5.1+cu118; platform_system != "Darwin"

# PyTorch for macOS (CPU-only)
torch; platform_system == "Darwin"
torchvision; platform_system == "Darwin"
torchaudio; platform_system == "Darwin"

# Platform-specific dependencies
pywin32==308; platform_system == "Windows"
sounddevice==0.5.1; platform_system != "Windows"

# Git-based dependencies
SAM-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4
